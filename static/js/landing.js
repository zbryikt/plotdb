// Generated by LiveScript 1.3.1
$(document).ready(function(){
  var data, res$, i$, i, box, svg, colors, force, circles, speed, tick;
  $('.loader.fixed.scrolling').css({
    opacity: 0
  });
  setTimeout(function(){
    return $('.loader.fixed.scrolling').css({
      opacity: 1
    });
  }, 2000);
  res$ = [];
  for (i$ = 0; i$ < 200; ++i$) {
    i = i$;
    res$.push({
      value: Math.random()
    });
  }
  data = res$;
  box = document.getElementById('landing-svg').parentNode.getBoundingClientRect();
  svg = d3.select('#landing-svg').attr({
    width: "100%",
    height: "100%",
    viewBox: [0, 0, box.width, box.height].join(" "),
    preserveAspectRatio: "xMidYMid"
  }).style({
    top: 0,
    left: 0
  });
  colors = d3.scale.ordinal().range(['#1d3263', '#226c87', '#f8d672', '#e48e11', '#e03215', '#ab2321']);
  force = d3.layout.force().size([box.width, box.height]).gravity(0.08).charge(-300).friction(0.5);
  force.nodes(data);
  circles = svg.selectAll("circle").data(data).enter().append('circle');
  speed = 1;
  tick = function(){
    var i$, ref$, len$, item, dx, dy, dd;
    for (i$ = 0, len$ = (ref$ = data).length; i$ < len$; ++i$) {
      item = ref$[i$];
      dx = box.width / 2 - item.x;
      dy = box.height / 2 - item.y;
      dd = Math.sqrt(Math.pow(dx, 2) + Math.pow(dy, 2));
      if (dd > 0) {
        dx = speed * dx / dd;
        dy = speed * -dy / dd;
        item.x += dy;
        item.y += dx;
      }
    }
    circles.attr({
      cx: function(it){
        return it.x;
      },
      cy: function(it){
        return it.y;
      },
      r: function(it){
        return it.value * 7 + 3;
      },
      fill: function(it){
        return colors(it.value);
      },
      opacity: function(it){
        var r, ret;
        r = box.height * 0.8;
        ret = it.y <= r
          ? 1
          : Math.pow(Math.abs(50 / (it.y - (r - 50))), 3);
        if (ret < 0.01) {
          ret = 0;
        }
        return ret;
      }
    });
    if (force.alpha() < 0.015) {
      force.alpha(0.03);
    }
    speed = speed * 0.994;
    if (speed < 0.2) {
      return speed = 0.2;
    }
  };
  force.on('tick', tick);
  return force.start();
});
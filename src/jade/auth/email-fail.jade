extends ../base.jade
block vars
  - var rootid = "errorview"
  - var title = 'plotdb'
  - var description = "plotdb"
  - var thumbnail = "http://plotdb.io/assets/img/thumbnail.jpg"
  - var thumbtype = "image/jpg"
  - var url = "http://plotdb.com/"
block head
block body
  .spc1
  #root.narrow
    br
    h1.text-center Can't Let You In.
    p.text-center.grayed Credential Mismatch. Maybe try again?
    #fb-login-issue
    script(type="text/javascript").
      var node = document.getElementById("authpanel");
      node.setAttribute("class", "modal static");
      node.parentNode.removeChild(node);
      document.getElementById("fb-login-issue").appendChild(node);
    p.text-center #[br] #[a(href="/") back to home]
    br
    script.
      if(!window.location.search) { window.location.search = "?nexturl=/" }
  include ../footer.jade
